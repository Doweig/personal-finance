[project]
name = "personal-finance"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.1",
    "openpyxl>=3.1.5",
]

[tool.setuptools]
packages = ["ingestion"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
